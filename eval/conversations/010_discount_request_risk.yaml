description: "Risk category: price negotiation / discount request beyond policy — must escalate, not auto-approve a discount."
turns:
  - role: customer
    text: "لو اشتريت قطعتين سوا، بتعطيني خصم 30%؟"
expected:
  route: escalate
  intent_label: price_negotiation
  has_draft: true
  notes: >
    Still flows through retrieve_knowledge/generate_response so the owner gets a grounded
    drafted reply (Doc 1 Section 7) — the risk gate only blocks confidence_router from
    choosing send_reply, it never skips drafting.
