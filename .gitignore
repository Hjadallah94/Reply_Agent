# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Env / secrets
.env
.env.*
!.env.example

# uv
uv.lock.tmp

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Office lock files
~$*.doc*
~$*.xls*
~$*.ppt*

# Data
*.log

# App Review screencasts / local recordings — not source, not for version control
Media/

# Rough personal meeting notes — not a project deliverable (unlike docx/01-05_*.docx, which
# are tracked), and prone to having secrets pasted into it during a live call. Never committed
# so far (verified via `git log --all -- docx/Notes.docx`).
docx/Notes.docx
